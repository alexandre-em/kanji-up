declare const enum UpdateKanjiProps {
	UPDATE_RADICAL,
	UPDATE_CHARACTER,
	UPDATE_REFERENCE,
}

declare const enum PAGINATION_LIMIT {
	LITTLE = 10,
	NORMAL = 20,
	ALOT = 50,
	ABUNDANT = 100,
	MAX = 200,
}
