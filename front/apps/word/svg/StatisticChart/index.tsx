import React from 'react';
import { Image } from 'react-native';

export default function StatisticChart({ width, height }: SvgProps) {
  return <Image style={{ width, height }} source={require('./undraw_statistic_chart_re_w0pk.svg')} />;
}
