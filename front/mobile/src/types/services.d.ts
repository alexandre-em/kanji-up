// abstract class AbstractFileService {
//   /** Read a file from path then returns the content */
//   read(path: string): Promise<string>;
//   /** Write a file from path then returns the path */
//   write(path: string, content: string): Promise<string | void>;
//   /** Upload a file from path then returns the url */
//   upload(path: string): Promise<string | void>;
//   /** Download a file from path then returns the content */
//   download(path: string, location: string): Promise<Blob | ArrayBuffer | string | void>;
// }
